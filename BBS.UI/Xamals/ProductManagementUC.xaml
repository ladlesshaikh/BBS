<UserControl x:Class="BBS.UI.Xamals.ProductManagementUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid AutoGenerateColumns="False" Height="226"
          HorizontalAlignment="Left" Margin="0,0,0,0"
          Name="dgProduct" VerticalAlignment="Top"  ColumnWidth="*"
          RowEditEnding="dgProduct_RowEditEnding"  CellEditEnding="dgProduct_CellEditEnding"
          SelectionChanged="dgProduct_SelectionChanged" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" x:Name="dgProductName">
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Description}" Header="Description" x:Name="dgProductDescription">
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Value}"  Header="Value" x:Name="dgProductSalary" >
                </DataGridTextColumn>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Update" x:Name="btnUpdate"
                                Click="btnUpdate_Click"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" x:Name="btnDelete"
                                Click="btnDelete_Click"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!--<TextBlock Height="55" HorizontalAlignment="Left" Margin="1,50,0,0"
               Name="textBlock1" VerticalAlignment="Top" Width="693"
                Text="WPF DataGrid for Update and Delete Operations"               
                TextAlignment="Center" FontFamily="Verdana" FontSize="28" />-->
    </Grid>
</UserControl>
